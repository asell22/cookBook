<h3>Post a Comment</h3>

<%= form_for [ @recipe, @comment ] do |f| %>
    <%= f.label :body %><br/>
    <%= f.text_area :body, class: "comment-body" %>
  </p>
  <p>
    <%= f.submit 'Submit', class: "comment" %>
  </p>
<% end %>
